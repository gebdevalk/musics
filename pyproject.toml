[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "myproject"
version = "0.1.0"
dependencies = [
    "mypy>=1.8.0",
]

[project.optional-dependencies]
dev = [
    "mypy>=1.8.0",
]
